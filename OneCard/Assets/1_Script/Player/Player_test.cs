using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player_test : MonoBehaviour
{
    [SerializeField] private List<GameObject> player_CardDeck;

    public List<GameObject> Player_CardDeck { get => player_CardDeck; set => player_CardDeck = value; }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
